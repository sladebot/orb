{
  "name": "orb-web",
  "version": "0.0.0",
  "private": true,
  "description": "Frontend tests for the Orb web dashboard (graph.js, app.js, style.css live under web/static).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "happy-dom": "^15.7.4",
    "vitest": "^2.1.2"
  }
}
